
    const toggleBtn = document.getElementById('toggleBtn');
    const navbarNav = document.getElementById('collapse');

    toggleBtn.addEventListener('click', ()=> {
        navbarNav.classList.toggle('show');
    });
